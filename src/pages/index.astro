---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.jsx';
import About from '../components/About.jsx';
import Experience from '../components/Experience.jsx';
import Projects from '../components/Projects.jsx';
import Contact from '../components/Contact.jsx';
---

<Layout 
	title="Portafolio Espacial - Desarrollador Móvil & Ciberseguridad"
	description="Explorador del ciberespacio especializado en desarrollo móvil seguro y arquitectura de sistemas. Defendiendo la última frontera digital."
>
	<div id="main-content" class="relative">


		<!-- Navigation -->
		<nav class="fixed top-0 left-0 right-0 z-40 backdrop-blur-sm border-b border-opacity-20" 
			 style="background: rgba(26, 26, 26, 0.1); border-color: rgba(255, 255, 255, 0.2);">
			<div class="max-w-6xl mx-auto px-6 py-4">
				<div class="flex justify-between items-center">
					<!-- Logo -->
					<a href="#" class="text-xl font-bold font-space hover:glow-effect transition-all duration-300">
						🚀 SPACE.DEV
					</a>

					<!-- Navigation Links -->
					<div class="hidden md:flex items-center gap-8 text-sm font-mono">
						<a href="#about" class="hover:glow-effect transition-all duration-300 hover:scale-110">
							PANEL_CONTROL
						</a>
						<a href="#experience" class="hover:glow-effect transition-all duration-300 hover:scale-110">
							MISIONES
						</a>
						<a href="#projects" class="hover:glow-effect transition-all duration-300 hover:scale-110">
							EXPEDICIONES
						</a>
						<a href="#contact" class="hover:glow-effect transition-all duration-300 hover:scale-110">
							COMUNICACIÓN
						</a>
					</div>

					<!-- Mobile menu button -->
					<button class="md:hidden p-2 rounded border border-white border-opacity-20 hover:glow-effect transition-all duration-300"
							onclick="toggleMobileMenu()">
						<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
						</svg>
					</button>
				</div>

				<!-- Mobile menu -->
				<div id="mobile-menu" class="hidden md:hidden mt-4 pt-4 border-t border-white border-opacity-20">
					<div class="flex flex-col gap-4 text-sm font-mono">
						<a href="#about" class="hover:glow-effect transition-all duration-300" onclick="toggleMobileMenu()">
							PANEL_CONTROL
						</a>
						<a href="#experience" class="hover:glow-effect transition-all duration-300" onclick="toggleMobileMenu()">
							MISIONES
						</a>
						<a href="#projects" class="hover:glow-effect transition-all duration-300" onclick="toggleMobileMenu()">
							EXPEDICIONES
						</a>
						<a href="#contact" class="hover:glow-effect transition-all duration-300" onclick="toggleMobileMenu()">
							COMUNICACIÓN
						</a>
					</div>
				</div>
			</div>
		</nav>

		<!-- Main Content -->
		<main class="relative z-10">
			<!-- Hero Section -->
			<Hero client:load />

			<!-- About Section -->
			<About client:load />

			<!-- Experience Section -->
			<Experience client:load />

			<!-- Projects Section -->
			<Projects client:load />

			<!-- Contact Section -->
			<Contact client:load />
		</main>

		<!-- Footer -->
		<footer class="relative z-10 py-8 px-6 border-t" style="border-color: rgba(255, 255, 255, 0.1);">
			<div class="max-w-6xl mx-auto">
				<div class="flex flex-col md:flex-row justify-between items-center gap-4">
					<div class="text-center md:text-left">
						<div class="font-bold font-space mb-2">🚀 SPACE.DEV</div>
						<div class="text-sm opacity-60 font-mono">
							Explorando el universo del código desde 2018
						</div>
					</div>
					
					<div class="flex items-center gap-6 text-sm font-mono opacity-60">
						<div class="flex items-center gap-2">
							<div class="w-2 h-2 rounded-full bg-green-400 animate-pulse"></div>
							<span>SISTEMA OPERATIVO</span>
						</div>
						<div>© 2025 • TODOS LOS DERECHOS RESERVADOS</div>
					</div>
				</div>
				
				<div class="mt-6 pt-4 border-t border-opacity-10 text-center text-xs opacity-40 font-mono" style="border-color: rgba(255, 255, 255, 0.1);">
					Construido con Astro, React, GSAP y OGL • Diseñado para la exploración espacial
				</div>
			</div>
		</footer>
	</div>

	<!-- Scripts -->
	<script>
		// Mobile menu toggle
		function toggleMobileMenu() {
			const menu = document.getElementById('mobile-menu');
			menu?.classList.toggle('hidden');
		}

		// Smooth scrolling enhancement
		document.addEventListener('DOMContentLoaded', () => {
			// Add smooth scrolling with offset for fixed nav
			document.querySelectorAll('a[href^="#"]').forEach(anchor => {
				anchor.addEventListener('click', function (e) {
					e.preventDefault();
					const targetId = this.getAttribute('href').substring(1);
					const target = document.getElementById(targetId);
					
					if (target) {
						const offsetTop = target.offsetTop - 80; // Account for fixed nav
						window.scrollTo({
							top: offsetTop,
							behavior: 'smooth'
						});
					}
				});
			});

			// Add loading screen fade out
			const loadingScreen = document.createElement('div');
			loadingScreen.className = 'fixed inset-0 bg-black z-50 flex items-center justify-center transition-opacity duration-1000';
			loadingScreen.innerHTML = `
				<div class="text-center">
					<div class="text-4xl mb-4">🚀</div>
					<div class="text-sm font-mono animate-pulse">INICIANDO SISTEMA...</div>
				</div>
			`;
			document.body.appendChild(loadingScreen);

			// Remove loading screen after 1 second
			setTimeout(() => {
				loadingScreen.style.opacity = '0';
				setTimeout(() => {
					document.body.removeChild(loadingScreen);
				}, 1000);
			}, 1000);
		});

		// Add scroll-based nav highlighting
		const observerOptions = {
			rootMargin: '-80px 0px -80px 0px',
			threshold: 0.1
		};

		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				const navLink = document.querySelector(`nav a[href="#${entry.target.id}"]`);
				if (entry.isIntersecting) {
					// Remove active class from all nav links
					document.querySelectorAll('nav a[href^="#"]').forEach(link => {
						link.classList.remove('text-glow');
					});
					// Add active class to current nav link
					if (navLink) {
						navLink.classList.add('text-glow');
					}
				}
			});
		}, observerOptions);

		// Observe all sections
		document.addEventListener('DOMContentLoaded', () => {
			document.querySelectorAll('section[id]').forEach(section => {
				observer.observe(section);
			});
		});
	</script>

	<!-- Add some additional CSS for better mobile experience -->
	<style>
		.text-glow {
			text-shadow: 0 0 10px currentColor;
		}

		/* Hide scrollbar but keep scrolling functionality */
		::-webkit-scrollbar {
			width: 8px;
		}

		::-webkit-scrollbar-track {
			background: transparent;
		}

		::-webkit-scrollbar-thumb {
			background: rgba(255, 255, 255, 0.2);
			border-radius: 4px;
		}

		::-webkit-scrollbar-thumb:hover {
			background: rgba(255, 255, 255, 0.4);
		}

		/* Improve mobile tap targets */
		@media (max-width: 768px) {
			nav a, button {
				min-height: 44px;
				display: flex;
				align-items: center;
				justify-content: center;
			}
		}

		/* Accessibility improvements */
		.sr-only {
			position: absolute;
			width: 1px;
			height: 1px;
			padding: 0;
			margin: -1px;
			overflow: hidden;
			clip: rect(0, 0, 0, 0);
			white-space: nowrap;
			border: 0;
		}

		.focus\:not-sr-only:focus {
			position: static;
			width: auto;
			height: auto;
			padding: 0.5rem 1rem;
			margin: 0;
			overflow: visible;
			clip: auto;
			white-space: normal;
		}
	</style>
</Layout>
